numpy>=2.0
pandas>=2.0
faiss-cpu>=1.7.4

# SigLIP2 (backend/models.py) -- joint text/image embedding space shared
# by every SigLIP2-backed leg (frame, ASR, caption, summary).
torch>=2.8.0
transformers>=4.57.0
pillow

tqdm>=4.60

# ASR / Caption fuzzy legs (backend/search/asr.py, caption.py) -- bulk-indexes
# transcript/caption text into a local Elasticsearch and runs a fuzzy match
# query against it.
elasticsearch>=8.0.0

# backend/ -- FastAPI app (see backend/main.py) serving both the JSON API
# and the static frontend/.
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
cachetools>=5.3
