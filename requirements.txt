numpy>=2.0
pandas>=2.0
faiss-cpu>=1.7.4

# ViCLIP-OT (minhnguyent546/ViCLIP-OT, arXiv:2602.22678) -- loaded via
# transformers.AutoModel(..., trust_remote_code=True). timm/pillow are
# needed because AutoModel instantiates the full model (image + text
# tower), even though this pipeline only calls encode_text at runtime.
torch>=2.8.0
torchvision>=0.23.0
transformers>=4.57.0
timm
pillow

streamlit>=1.30

tqdm>=4.60

# old_version/ (the prior CLIP ViT-B/32 pipeline, kept for reference only
# and not run by anything above) needs its own, different dependency set --
# see git history for the original requirements.txt if it's ever run again.
