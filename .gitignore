.venv/
.idea/
.claude/
__pycache__/
*.pyc

*.log

# Stray local batch files stay out, but the launcher scripts are how the
# team actually starts the app -- a fresh clone needs them, so they're
# un-ignored by name below.
*.bat
!run_768.bat
!run_1152.bat
!run_1536.bat
!_run_common.bat
!open_when_ready.bat
!stop_routing101.bat

# Generated by pipeline/index_pipeline.py -- reproducible, and the .index
index/

docs/
old_version/

CLAUDE.md
# ARCHITECTURE.md is tracked again -- it's teammate-facing reference docs
# now (moved out of README.md), not a Claude-only working file like
# CLAUDE.md above.
